# Сессия 7 — v0.4.0: структурный рефакторинг под расширение SMM

**Дата:** 2026-05-26
**Длительность:** ~2 часа (Claude в чате, без отдельного Code-агента)
**Версия после сессии:** **v0.4.0**
**Статус:** Код готов, импорт-smoke прошёл, тег поставлен. UI-проверка в браузере — отдельно.

---

## Цель сессии

Подготовить структуру проекта под стратегическое расширение:
- Автопостинг по графику на сервере (после v0.4 деплой)
- SMM-каналы помимо VK (Telegram сначала, потом YouTube/TikTok)
- Автоматический сбор и обработка источников (в перспективе)

Параллельно — навести порядок в локальной папке: вынести бренд-ассеты из кодовой папки, разделить активные/архивные ТЗ, сгруппировать docs.

## Что сделано

### Структура папок

**Удалено:**
- `lora.db` в корне (пустой дубль, рабочий в `data/`)
- `test_assets/` (один файл — перенесён в `tests/fixtures/`)
- `.pytest_cache/` (мусорный кэш)

**Перемещено:**
- `core/assets/` → `assets/lora/` (75 МБ оригиналов PNG из кодовой папки в бренд-папку)
- `tasks/CLAUDE_CODE_*.md` (8 шт) → `tasks/archive/`
- `docs/PROJECT_REVIEW_2026-05-10.md` → `docs/reviews/`
- `docs/vk-oauth-research.md` → `docs/research/`
- `docs/server-deployment-vk.md` → `docs/deployment/`

**Создано:**
- `references/` — бренд-материалы (тон голоса, гайдлайны рубрик)
- `briefs/` — короткие записки для Claude в чате
- `tmp/` — эфемерные файлы (gitignored)
- `tasks/active/` — пустая, под текущее ТЗ
- `tasks/archive/` — для закрытых ТЗ
- `docs/research/` + `docs/deployment/` + `docs/reviews/` — подгруппы
- `data/inbox/video/` + `data/inbox/text/` — для будущего автосбора и видео из Kling
- `data/exports/` — для отчётов аналитики
- `data/content_calendar/` — для контент-плана
- `assets/lora/` — переезд бренд-ассетов из `core/`

### Декомпозиция `core/`

**Подпакеты:**
- `core/publishers/` — `base.py` (BasePublisher, PublishResult, PublishError) + `vk.py` (VKClient + VKPublisher) + `vk_oauth.py` + `telegram.py` (реальная реализация через httpx, ждёт ключей) + `youtube.py` + `tiktok.py` (placeholder с docstring-планом)
- `core/generators/` — `llm_client.py`, `image_clients.py`, `prompt_generator.py`, `lora_references.py`, `prompts.py`
- `core/storage/` — `db.py`, `models.py`
- `core/ingest/` — пустой пакет + `README.md` с планом источников (RSS, Reddit, конкуренты)
- `core/analytics/` — пустой пакет + `README.md` с планом метрик

**Осталось на верхнем уровне `core/`:**
- `scheduler.py` (cross-cutting)
- `logging_utils.py` (cross-cutting)

**Обновлено импортов:** 11 файлов (app.py, routes/api.py, scripts/get_vk_token.py, tests/test_vk_oauth.py + взаимные внутри core/).

### Публикаторы

**Базовый интерфейс** `core/publishers/base.py`:
- `BasePublisher` — абстрактный класс с методами `is_configured`, `publish_text`, `publish_with_image`, `publish_with_video`
- `PublishResult` (TypedDict) — единый формат возврата: platform/post_id/post_url
- `PublishError` — общее исключение, наследуемое от Exception

**Реестр** `core/publishers/__init__.py`:
- `get_publisher(name)` — фабрика с ленивым импортом
- `available_publishers()` — список со статусом каждого

**VK** — `VKClient` + адаптер `VKPublisher`, тонкая обёртка. `VKAPIError` теперь наследуется от `PublishError` для общего ловителя в scheduler/CLI.

**Telegram** — полная реализация Bot API через httpx:
- `sendMessage` (текст до 4096)
- `sendPhoto` + caption (до 1024) — для будущей текст+картинка
- `sendVideo` + caption (до 1024) — для будущей текст+видео
- Без `TELEGRAM_BOT_TOKEN` и `TELEGRAM_CHANNEL_ID` отвечает «не настроен» — UX-понятно.
- URL поста собирается из `chat.id` + `message_id`. Для приватных каналов `https://t.me/c/<short>/<id>`.

**YouTube / TikTok** — placeholder-классы с `IMPLEMENTED = False` и docstring-планом подключения (что нужно: Google Cloud / TikTok Developer, OAuth, app review). Не работают, но не падают при загрузке.

### CLI `manage.py`

Команды через argparse:
- `status` — версия + публикаторы + кол-во постов
- `list-posts [--status X] [--limit N]` — табличный список
- `show-post <id>` — детали
- `generate-post <rubric> [--topic ...]` — Claude → draft в БД
- `publish <id> [--platform vk|telegram|all]` — публикация через factory
- `schedule <id> <iso-datetime>` — APScheduler

Включает `sys.stdout.reconfigure(encoding="utf-8")` чтобы кириллица читалась в Windows-консоли.

### Мультиплатформенный scheduler (v0.4)

**Изменения в схеме БД:**
- Колонка `Post.target_platforms` (TEXT, JSON-список, default `'["vk"]'`) — какие платформы публикуют этот пост
- Таблица `post_publications` — лог попыток по каждой платформе: post_id / platform / post_id_at_platform / post_url / success / error_message / published_at
- Миграция `migrate_to_v0_4()` — ALTER TABLE с проверкой `PRAGMA table_info`, идемпотентно

**Изменения в `_do_publish`:**
- Итерируется по `post.get_target_platforms()` через `get_publisher(platform)`
- Каждая попытка пишется в `post_publications`
- Статус Post: `published` если хотя бы одна платформа отработала; `publish_failed` если все упали
- Legacy: для VK дублируем `vk_post_id`/`vk_post_url` в Post для backward-compat с UI

### Заглушка Telegram

В `.env.example` добавлены переменные:
- `TELEGRAM_BOT_TOKEN` — токен от @BotFather
- `TELEGRAM_CHANNEL_ID` — `@username` или `-100xxxxxxxxxx`

Пустые ключи → `TelegramPublisher.is_configured()` возвращает False → CLI/scheduler корректно пропускают.

В `config.py` добавлены `TELEGRAM_API_BASE`, `TELEGRAM_TEXT_LIMIT`, `TELEGRAM_CAPTION_LIMIT`.

### Документация

- `README.md` — обновлена секция структуры (актуальная иерархия), добавлен раздел CLI, переписан roadmap.
- `PROJECT_LOG.md` — строка v0.4.0.
- `docs/sessions/README.md` — добавлена ссылка на этот файл (в Фазе 7 ниже).
- README в каждой новой папке: `references/`, `briefs/`, `tasks/`, `tasks/archive/`, `tests/fixtures/`, `docs/research/`, `docs/deployment/`, `docs/reviews/`, `core/ingest/`, `core/analytics/`.

---

## Ключевые решения

1. **Не подключаемся к utopy.ai.** Discovery показал: это российский веб-агрегатор без публичного API, без MCP, без Skill. ProxyAPI у нас уже даёт доступ к тем же моделям (gpt-image-2, Kling) через нормальный REST. Selenium-обход не рекомендую — ToS-violation, нестабильно.

2. **Telegram — реальная реализация, не stub.** Несмотря на отсутствие канала сейчас, модуль написан с полной логикой Bot API. При появлении токена работает сразу без правок кода. Заглушка — только в смысле что ключей в `.env` нет.

3. **YouTube/TikTok — placeholder, не реализация.** OAuth-flow для них слишком тяжёлый чтобы делать вслепую: требуют developer account, app review (особенно TikTok), что без реальной потребности — потеря времени. Документация плана в docstring модуля.

4. **`Post.target_platforms` как JSON-text vs отдельная таблица.** Выбрал JSON-колонку — проще для текущей задачи (мультиплатформенный sched), SQLite это терпит. Если понадобится индекс «найди все посты для Telegram» — добавим отдельную таблицу.

5. **`PostPublication` отдельной таблицей, а не дублированием полей в Post.** Чтобы добавление новой платформы не требовало миграции схемы. Legacy `vk_post_id`/`vk_post_url` в Post — для backward-compat с UI; со временем UI можно мигрировать на `post_publications`.

6. **VKAPIError наследуется от PublishError.** Это даёт `except PublishError` в scheduler/CLI работающий и для VK-специфичных ошибок, не ломая существующий `except VKAPIError` в routes/api.py.

7. **Тег v0.3.3 ставится ДО рефакторинга.** Чистая точка отката. Если в v0.4.0 что-то сломалось — `git checkout v0.3.3` возвращает к рабочему состоянию.

8. **Бренд-ассеты вне `core/`.** 75 МБ оригиналов PNG — это не код. `assets/lora/` корневая папка снимает 75 МБ концептуально с кодовой части. PNG в `.gitignore`, оптимизированные JPG (~1 МБ) в репо как production-ассеты.

---

## Возникшие проблемы

- **Edit-инструмент требует Read перед редактированием перемещённых файлов** — после `git mv` файлы считаются «новыми». Пришлось делать Read для каждого затронутого файла перед Edit.
- **Невидимые символы в `.env.example`** — первая попытка Edit с многострочным шаблоном не нашла совпадение, видимо из-за невидимых байтов. Пересчитал по короткому уникальному фрагменту.
- **Кириллица в Windows-консоли** — sys.stdout по умолчанию cp1251, в Python 3.9+ можно `reconfigure(encoding='utf-8')`. Добавил в `manage.py` блок реконфигурации.

---

## Артефакты

**Новые файлы:**
- `manage.py` (CLI)
- `core/publishers/__init__.py` (registry)
- `core/publishers/base.py` (BasePublisher)
- `core/publishers/telegram.py` (полная реализация Bot API)
- `core/publishers/youtube.py` (placeholder)
- `core/publishers/tiktok.py` (placeholder)
- `core/generators/__init__.py`
- `core/storage/__init__.py`
- `core/ingest/__init__.py` + `README.md`
- `core/analytics/__init__.py` + `README.md`
- README.md в `tests/fixtures/`, `tasks/`, `tasks/archive/`, `docs/research/`, `docs/deployment/`, `docs/reviews/`, `references/`, `briefs/`

**Перемещения (git mv):**
- `core/vk_client.py` → `core/publishers/vk.py`
- `core/vk_oauth.py` → `core/publishers/vk_oauth.py`
- `core/llm_client.py` → `core/generators/llm_client.py`
- `core/image_clients.py` → `core/generators/image_clients.py`
- `core/prompt_generator.py` → `core/generators/prompt_generator.py`
- `core/lora_references.py` → `core/generators/lora_references.py`
- `core/prompts.py` → `core/generators/prompts.py`
- `core/db.py` → `core/storage/db.py`
- `core/models.py` → `core/storage/models.py`
- `core/assets/optimized/*.jpg` → `assets/lora/optimized/*.jpg`
- 8× `tasks/CLAUDE_CODE_*.md` → `tasks/archive/`
- 3× `docs/*.md` → `docs/{research,deployment,reviews}/`
- `test_assets/test_video.mp4` → `tests/fixtures/test_video.mp4`

**Изменения в моделях:**
- `Post.target_platforms` (новая колонка) + `get_target_platforms()` (хелпер парсинга JSON)
- `PostPublication` (новая модель)

**Изменения в config.py:**
- Новые константы пути: `ASSETS_DIR`, `LORA_ASSETS_DIR`, `LORA_OPTIMIZED_DIR`, `INBOX_VIDEO_DIR`, `INBOX_TEXT_DIR`, `EXPORTS_DIR`, `CONTENT_CALENDAR_DIR`, `TMP_DIR`
- Telegram: `TELEGRAM_BOT_TOKEN`, `TELEGRAM_CHANNEL_ID`, `TELEGRAM_API_BASE`, `TELEGRAM_TEXT_LIMIT`, `TELEGRAM_CAPTION_LIMIT`
- Все рабочие папки создаются при старте через цикл `for _d in (...) _d.mkdir(...)`

---

## Финальная приёмка

### Smoke-тесты (прошли)
- [x] `python -c "import app"` — все импорты разрешились без ошибок
- [x] `init_db()` — миграция v0.4 применилась («добавлены колонки ['target_platforms']»)
- [x] `python manage.py status` — таблица публикаторов, статусы постов, корректная кириллица
- [x] `python manage.py list-posts --limit 3` — табличный вывод
- [x] `available_publishers()` — все 4 публикатора видны со статусами

### НЕ проверено в этой сессии (на следующую)
- [ ] Браузерная проверка Flask UI — должен работать как раньше (общий код тот же)
- [ ] Реальная публикация в VK через CLI `manage.py publish` — пока не дёргали
- [ ] Реальная публикация в VK через Flask UI после рефакторинга
- [ ] Запуск scheduler-задач (создать пост со status=scheduled, проверить что publish прошёл)

### Что осталось / задачи на следующую сессию

**UX (с предыдущей сессии):**
1. Упрощение кнопок генерации изображения (3 → 2)
2. Сравнение «с/без референса Лоры» на одной идее — артефакт для документации

**Backend v0.4 follow-up:**
1. **UI для выбора `target_platforms`** при создании поста — пока хардкод дефолта `["vk"]`. Когда появится Telegram-канал и активируется второй публикатор, нужен чекбокс «опубликовать в Telegram».
2. **Реальный Telegram-канал** — после регистрации заполнить `.env`, протестировать `manage.py publish <id> --platform telegram` на тестовом сообщении.
3. **Перенос UI на `post_publications`** — сейчас UI читает `Post.vk_post_id`/`vk_post_url`. Для отображения мультиплатформенной истории нужно перейти на эту таблицу.
4. **Structured output от Claude** — переход с парсинга текста на JSON-schema response (для надёжной автоматизации).

**Roadmap пункты (без изменений):**
1. Маркетинговое оформление VK-сообщества (ручное Dr. Nik)
2. Запуск автопостинга по графику (контент-план)
3. Backend стабильность — Alembic, CSRF, retry, тесты CRUD
4. LinguaAI integration
5. VPS deploy
6. Аналитика VK/Telegram

---

## Команды для следующей сессии

```cmd
cd C:\Users\nlovt\claude_projects\lora-content-studio
venv\Scripts\activate
python app.py                          # Flask UI (как раньше)
python manage.py status                # CLI
python manage.py generate-post word_of_day
```

---

## Расходы AI за сессию

- Subagent для utopy.ai discovery — ~5 web search вызовов, 2 минуты
- ~30 операций с файлами (Read/Edit/Write/Bash) на структуру
- ProxyAPI Claude вызовов: 0 (не дёргали Claude через приложение)
